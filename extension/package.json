{
  "name": "jarvis-site-intel-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome side-panel extension: crawls the active site via the crawler API and renders a HUD-style page tree + workflow picker",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.2"
  }
}
